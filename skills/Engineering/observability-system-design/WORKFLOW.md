---
name: observability-system-design
description: 当用户设计或评审长期运行系统的监控、可观测性、告警聚合、故障修复、自愈闭环，或需要决定复用现有能力还是登记缺口并自建时使用。
---

# Purpose

建设服务于系统目标的“感知 -> 判断 -> 修复 -> 验证 -> 演进”闭环，而不是从指标、告警规则或监控组件出发堆功能。

# When to Use

- 设计或评审平台、服务、Agent/runtime、异步链路的监控与故障感知体系。
- 告警无法表达主子链路、节点顺序、生命周期阶段、根因聚合或恢复状态。
- 需要把告警、诊断、人工修复和自动恢复设计成连续闭环。
- 已有云监控、dashboard、告警触达或恢复能力，需要判断复用、适配、补缺口还是自建。
- 要形成可服务不同系统、不同平台的可复用观测方法。

# When NOT to Use

- 只查询一次日志、定位一次事故，且不改变长期感知或恢复能力。
- 已有明确契约，只机械增加一个指标、面板或告警阈值。
- 只调整 dashboard 视觉布局，不改变事实、策略、事件或修复语义。

# Inputs

- 系统定位、关键用户旅程、成功标准、SLO/可接受降级和修复权限边界。
- 运行拓扑、系统领域、节点顺序、生命周期阶段、依赖和 owner。
- 已有事实源、监控/告警/通知/恢复能力、运行约束和采用证据。
- 失败模式、历史事故、当前盲区、恢复动作、验证方式和运维负担。

# Decision Principles

- 系统建设第一：先定义必须感知和恢复的系统结果，再决定指标、策略、组件和供应方。
- 分离 `Fact -> Observation -> Policy -> Incident -> Action -> Verification`：生产者只上报可证明的原子事实；时间窗口、基线、阈值、异常、健康度和告警由策略层动态推导，不回写成第二事实源。
- 监控是拓扑、生命周期、因果和恢复状态的联合模型，不用指标数量代表完备度。
- 显式覆盖“没有发生”：无数据、未启动、未结束、卡住、乱序、恢复未完成和观测链路自身失效都要有判定语义。
- 事件围绕影响和因果聚合：保留根事件、父子关联、领域与节点顺序、阶段、策略证据和恢复进度；单个症状不是默认告警主干。
- 先盘点真实已有能力并做适配性验证；能满足目标就复用，不能满足就登记可追踪缺口。只有缺口达到明确建设触发条件时才自建。
- 通用模型和系统画像分离：稳定语义、决策门禁与工件可跨系统复用；具体供应方、资源 ID、阈值和部署拓扑留在项目侧。
- 观测链路默认与业务主链路隔离、异步且有界，监控故障不能拖垮被监控系统；安全、合规等必须同步阻断的例外要显式定义失败语义。
- 自愈不是无条件自动执行。动作必须有适用条件、权限、幂等或可逆性、影响预算、互斥/限流、验证、回滚和人工升级路径。

# Workflow

1. **Position**：按 `系统定位 > 系统架构 > 功能设计 > 领域架构 > 模块设计` 冻结系统目标、关键旅程、SLO、不可接受结果、修复权限和非目标。
2. **Inventory**：盘点运行拓扑、事实源及已有观测、dashboard、告警、通知、工单、恢复能力；逐项标记 `reuse / adapt / gap / build-candidate / N/A`，附真实采用证据和 owner。
3. **Model**：建立系统观测画像，至少表达实体与父子/依赖关系、系统领域、节点顺序、生命周期阶段、终态、超时和 correlation keys。
4. **Facts**：定义事实目录和最小公共 envelope；区分状态事实、事件事实和度量事实，明确时间、来源、身份、因果、结果与数据质量，不在 producer 中固化告警结论。
5. **Coverage**：按关键旅程 × 拓扑节点 × 生命周期阶段 × 失败模式建立覆盖矩阵；同时检查显式失败、缺失/卡住、乱序/冲突、退化/饱和、重试耗尽、恢复失败和 telemetry failure。
6. **Observe And Decide**：由事实生成可重算的 observation，再用阶段、窗口、基线/SLO、阈值、异常、no-data 和抑制条件形成 policy；阈值必须披露来源与适用范围。
7. **Aggregate Incidents**：围绕一次系统影响或根执行聚合症状，输出事件主干、父子树、领域与节点顺序、命中阶段、策略证据、影响、当前诊断和恢复状态；定义去重、合并、抑制、路由、升级与恢复通知。
8. **Repair Loop**：为高价值事件闭合 `detect -> diagnose -> select -> authorize -> execute -> verify -> rollback/escalate`；按观察、建议、审批执行、有界自愈分级推进，不跨过安全门禁追求自动化率。
9. **Deliver Vertically**：先选择当前系统最关键的一条用户旅程，贯通事实、观察、策略、事件、通知、修复和验证，再按覆盖缺口扩展；不先建设脱离真实消费方的通用平台或组件全集。
10. **Verify And Evolve**：用故障注入、回放或受控演练验证检测率、误报/漏报、发现/恢复时间、聚合质量、动作安全和恢复确认；把未覆盖项写入缺口登记。只有相邻系统验证了稳定性后，才把项目模式提升为通用能力。

# Checklist

- 方案是否从系统结果和恢复目标出发，而不是从指标或组件数量出发。
- 事实、观察、策略、事件、动作和验证是否 owner 清楚、可追溯且没有双事实源。
- 主子关系、领域、节点顺序、阶段、无数据和恢复状态是否可表达。
- 已有能力是否经过真实盘点和适配性验证；自建项是否有缺口证据、真实 consumer、owner、触发条件和退出/回滚方案。
- 告警是否按因果和影响聚合，并披露策略证据，而不是重复通知每个症状。
- 自动修复是否有授权、幂等/可逆、影响预算、验证和人工升级路径。
- telemetry、策略引擎或恢复执行器失效时，业务系统是否仍有明确且安全的行为。
- 验收是否覆盖一条真实端到端旅程及故障/恢复演练，而不只验证“有数据”或“能触达”。

# Escalation

- 形成正式需求、SLO 和验收边界：`skills/Requirements/requirements-brief/WORKFLOW.md`
- 涉及公共 API、事件 envelope、tool 或跨系统契约：`skills/Engineering/interface-contract-audit/WORKFLOW.md`
- 需要定位现有事实源、运行 owner 和影响范围：`skills/Navigation/codebase-navigation/WORKFLOW.md`
- 需要核对供应方当前能力或业界实践：`skills/Research/source-grounded-research/WORKFLOW.md`

# References

- `references/system-method.md`
