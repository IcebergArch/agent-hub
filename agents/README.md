# Agents

这里放工具适配说明，不放项目知识和长期报告。

- `codex.md`：Codex 使用约定。
- `cursor.md`：Cursor 使用约定。

通用规则写在根目录 `AGENTS.md`。可复用流程写入 `skills/`，例如 Agent Team 路由维护在 `skills/agent-team-router/SKILL.md`。如果某条规则多个工具都需要，放回 `AGENTS.md`，不要在这里重复维护。
