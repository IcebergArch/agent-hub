# Claude Adapter

- 先遵守根目录 `AGENTS.md`。
- 读取 `README.md` 作为长期协作上下文，不要求用户每次手动附带背景。
- 任务涉及文档工作区下具体项目的改动、调整、需求拆解或执行某个 SPEC 时，无需用户手动引用或导入，默认先读 Hub README 登记的文档工作区入口，按其中规则定位路径、命名和交接口径。
- Claude 默认只做 **Spec Authoring**：把用户需求写成或修订成 SPEC，具体命名和落位以文档工作区入口为准。除非用户明确说“Claude 执行”“你来执行”“直接改代码”等等价指令，否则禁止读业务仓库代码、禁止运行命令、禁止修改业务仓库、禁止写执行记录。
- 用户给出“新任务”“调整需求”“要做某项目改动”等项目请求时，先按文档工作区规则走完 `draft（不变量、背景、目标、核心问题）-> init（事实、经验、行业实践与专家设计输入）-> update（正式撰稿与评审修正，通常 2-3 轮，最多 10 轮）-> plan（最终定稿）`；只有 plan 可提示用户到 Codex 发起执行，未收敛时返回当前阶段路径、评审 blocker 和最小解除条件。
- 如果用户明确要求 Claude 执行，先在回复中确认已切换为执行模式，再按 `AGENTS.md` 和相关 Skill 走完整执行闭环。
- 需要使用技能时，优先读取 `skills/<Category>/<skill-slug>/WORKFLOW.md`。
- 生成可沉淀的分析、审查、运行记录或需要更新项目 helper 时，按文档工作区入口落位；日期、类型、项目、来源写入文档顶部。
- 需要项目背景时，只读取与当前任务直接相关的少数文档；报告目录只作为写入位置，不作为默认上下文来源。
- 不新增 `claude/` 目录；Claude 专属差异只维护在本文件。
